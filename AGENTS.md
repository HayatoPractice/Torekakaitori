# ==============================================================================
# 👤 AGENTS.md — トークン削減 & AI行動規範指示書 (Context Guardian)
# ==============================================================================
# このファイルは、AIエージェント（Antigravity, Claude Code, VSCode等）の
# 行動を自己規律し、無駄なトークン消費を最大90%削減するための最優先ルールです。
# セッション開始時に必ず本ファイルを読み込み、すべての行動に適用してください。

---

## ▌ 0. 超重要：大原則（エスケープハッチ）

> [!IMPORTANT]
> **「正確性（Quality）はトークン節約に勝る」**
> トークン節約は極めて重要ですが、それによってバグを埋め込んだり的外れな回答をしては本末転倒です。
> **以下の場面では、トークンを恐れずに必要な生のコードやコンテキストを大胆に要求・読み込みしてください：**
> * 実際にコードを編集・上書きするファイルを特定したとき
> * 複雑なバグのデバッグ時で、スタックトレースの全容が必要なとき
> * アーキテクチャの根幹に関わる設計判断を行うとき

---

## ▌ 1. 静的フィルタリングの厳守

1. **無視ファイルの尊重**:
   * プロジェクトルートにある `.antigravityignore`、`.geminiignore`、`.aiexclude` に定義されているフォルダ・ファイル（`node_modules`, `dist`, バイナリ等）は、指示がない限り絶対に読み込み・スキャン対象に含めないでください。
2. **大容量ファイルの事前確認**:
   * 1ファイルが **50KB（目安：約1,000行）** を超えるテキストファイル、またはバイナリの可能性が高いファイルを読み込む際は、まずファイルサイズやファイル構造を先に確認し、一括読み込みを避けてください。

---

## ▌ 2. 段階的ファイル読み込み (Read-in Minimization)

AIがプロジェクト内の他のファイルを調査する際は、以下のステップを厳守してください。

```
[指示を受ける]
  │
  ▼
【STEP 1】まず「ディレクトリ構造」か「DOCS/PROJECT_STATE.md」を読み、あたりをつける。
  │
  ▼
【STEP 2】使い方や依存関係を調べる際は、まず「scripts/skeletonizer.py」を使用し、
          対象ファイルのシグネチャ（構造定義のみ）を読み込む。（生ファイルを読まない）
  │
  ▼
【STEP 3】編集・修正対象となるファイルが完全に特定されたら、
          その対象ファイル「のみ」を生コード（ロジック込み）で読み込む。
```

---

## ▌ 3. スケルトン化の徹底とコード破壊の防止

1. **skeletonizer.py の使用**:
   * 関数の使い方や型定義だけを知りたい場合は、必ず以下のコマンドを使用して構造のみを読み込んでください。
     ```bash
     python3 ./scripts/skeletonizer.py <対象ファイルパス>
     ```
2. **コード破壊の防止（上書き保存の禁止）**:
   * `skeletonizer.py` の出力は **シグネチャ確認用（閲覧専用）** です。
   * **警告ヘッダー `[WARNING: SKELETONIZED FOR CONTEXT]` が含まれるコードをベースにして、そのまま生ファイルに上書き保存することは厳禁です。** 内部のロジックがすべて消失し、コードを破壊してしまいます。編集を行う際は、必ず生のファイルを読み込んでから編集を行ってください。

---

## ▌ 4. エラーログ・履歴のインテリジェント圧縮

1. **ログの切り詰めルール**:
   * テスト実行やビルドで100行を超えるエラーログが出た場合、ログ全体を会話履歴（コンテキスト）に残すと、次回以降のターンでトークンが指数関数的に肥大化します。
   * モデルにログを渡す際は、**「最初のエラーメッセージ」**、**「Exceptionの種類」**、および**「スタックトレースの最深部と最上部（最初と最後の5行ずつ）」**など、根本的な原因となる行のみをインテリジェントに抽出して残し、中間の無駄なログは履歴からクリア（GC）してください。
2. **会話の定期的な要約**:
   * 会話が長期化し、ツール実行履歴が肥大化してきた場合は、`CONTEXT_BRIDGE.md` を更新した上で、これまでの実行プロセスの「要約のみ」を履歴に残し、過去の巨大な生ログやコードダンプを消去するよう自己管理してください。

---

---

AGENTS.md v1.0 — 🔒不変層。この規範はすべてのAIエージェントの行動を律する文脈の番人です。
