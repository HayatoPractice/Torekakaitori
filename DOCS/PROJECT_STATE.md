📍 PROJECT_STATE.md — 開発現在地ダッシュボード v1.0（アプリ特化版）
このファイルは常に最新状態を保つ。セッション開始時に必ず読む。
層：🔄 随時更新層（PMが変更）
更新頻度：セッションごとに確認・必要に応じて更新

▌変更履歴
バージョン  日付    変更内容                                        変更者
v1.0        初版    ビジネス版から分離・アプリ開発に特化して新規作成  秘書

▌最終更新
更新日時：[YYYY-MM-DD]
更新者：[PM / 秘書]
今回の変更内容：[1行で]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
▌アプリ情報（社長が記入・随時更新）
app_name:             # アプリ名
app_type:             # Webアプリ / ツール / スクリプト / その他
purpose:              # このアプリが解決すること
target_user:          # 誰が使うか
current_phase:        # Phase 0〜2
constraints:          # 制約・前提条件

▌開発情報（セッション開始時に必ず確認する）
# --- 技術スタック ---
tech_stack:
  frontend:           # 例：Next.js 14 / TypeScript
  backend:            # 例：Supabase / Edge Functions
  database:           # 例：PostgreSQL（Supabase）
  storage:            # 例：Cloudflare R2
  hosting:            # 例：Vercel
  auth:               # 例：Supabase Auth
  ai_api:             # 例：Gemini API / Claude API
  gas:                # GAS使用の場合に記入
  other:              # その他ライブラリ・外部サービス

# --- アーキテクチャ ---
architecture:         # 例：App Router / Server Components中心
state_management:     # 例：Zustand / React Context
api_style:            # 例：REST / tRPC / GraphQL

# --- 開発ルール ---
coding_language:      # 例：TypeScript（any禁止）
naming_convention:    # 例：コンポーネントはPascalCase
test_policy:          # 例：ユニットテスト必須 / E2Eはデモで実施
commit_convention:    # 例：feat / fix / chore / docs

# --- 環境情報 ---
env_dev:              # 開発環境のURL・ポート等
env_prod:             # 本番環境（社長承認後のみデプロイ・操作は社長が実施）
env_notes:            # 環境変数の管理方針

# --- テスト方針 ---
unit_test_policy:     # 例：関数単位で正常系・異常系を必ず確認
e2e_test_policy:      # 例：デモフェーズでAntigravityを使用して自動実施
regression_policy:    # 例：fix後に依存する既存機能を必ず確認

# --- パフォーマンス基準（社長が上書き可）---
perf_page_load:       # ページ初期読み込み目標（デフォルト：3秒以内）
perf_api_response:    # API応答時間目標（デフォルト：500ms以内）
perf_bundle_size:     # バンドルサイズ上限（デフォルト：250KB/gzip後）

# --- UI/UX方針 ---
responsive_policy:    # 例：モバイルファースト（375px / 768px / 1024px対応）
accessibility_level:  # 例：WCAG 2.1 Level A（最低限）
multilingual_policy:  # A：日本語のみ / B：多言語化考慮設計 / C：多言語対応
                      # 未記入の場合はBを採用する

# --- 状態管理方針 ---
state_management_rule: # グローバル：認証・複数画面跨ぎ / ローカル：UI状態・フォーム

# --- キャッシュ戦略 ---
cache_static:         # 静的アセットのキャッシュ期間（デフォルト：1週間）
cache_api:            # APIキャッシュ期間（デフォルト：5分）
cache_db:             # DBクエリキャッシュ（デフォルト：60秒）

# --- 現在の実装状況（PMがセッションごとに更新）---
current_branch:       # 現在の作業ブランチ
last_completed:       # 最後に完了した実装タスク
open_issues:          # 未解決の実装課題（件数）
implement_loop:       # 設計 / 実装 / 検証 / デモ待ち / 品質チェック待ち / 修正 / 完了

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
▌現在の状況サマリ
現フェーズ：[Phase N：フェーズ名]
フェーズ開始日：[日付]
タイムアウト目安日：[日付]（1.5倍超えたら秘書が課題サマリを作成）
アクティブチーム数：[N]チーム（常駐3チーム除く）
今週のフォーカス：[1行で]
実装ループ現在地：[設計/実装/検証/デモ待ち/品質チェック待ち/修正/完了]
開発完了フロー現在地：[STEP 1〜6 / 未開始]
CONTEXT_BRIDGE.md：[最終更新 YYYY-MM-DD / 未作成]
デモ状況：[未実施 / 自動デモ完了 / 手動デモ完了 / 要修正]

▌健全性スコア（PMが週次更新）
進捗：✅完了[N]件 / 🔄中間完了[N]件 / ⏳待機[N]件 / 🚨ブロッカー[N]件
技術的負債：高[N]件 / 中[N]件 / 低[N]件
REC記録：今週[N]件（バグ[N] / 判断ミス[N] / 修正指示[N]）
セキュリティ：全項目クリア / 要確認（[N]件）
デモ通過：[未実施 / STEP[N]まで通過 / 全項目通過]

⚠️ 警戒シグナル（該当する場合は記載）：
  □ 同じ箇所のfixが3回以上繰り返されている
  □ 同じ役割から3週連続でブロッカーが報告されている
  □ セキュリティチェックで未解消の問題がある
  □ 技術的負債「高」が未解消のままフェーズ移行しようとしている
  □ 本番環境への変更が社長承認なしに実施されようとしている

▌確認待ちリスト（PMが管理）
【要確認】（72時間以上経過したものは★でマーク）
  ★ [優先度高・要判断] BDR-XXX：[内容] 送信日：[日付]（[N]時間経過）
    [優先度中・リスク] BDR-XXX：[内容] 送信日：[日付]

確認待ち件数：[N]件　うち72時間超：[N]件

▌タスクボード（PMが管理）
✅ フェーズ真の完了（社長承認済み）
  - [タスク名]（[役割名]）

🔄 マイルストーン達成（引き継ぎ待ち）
  - [タスク名]（[役割名]）→ 引き継ぎ先：[役割名] 状況：[未着手/着手中]

🔁 実装ループ中（implement → verify → fix）
  - [タスク名]（[役割名]）現在地：[設計/実装/検証/修正] fix回数：[N]回

🎬 デモ待ち（verify完了・デモ未実施）
  - [タスク名]（[役割名]）デモファイル：[demo/パス]

🔍 品質チェック待ち（デモ通過・秘書チェック未実施）
  - [タスク名]（担当：秘書）

⏳ 待機中
  - [タスク名]（[役割名]）前提：[条件]

🚨 ブロッカー
  - [内容]（[役割名]）対応：[誰が・何をする]

📋 持ち越しタスク（前フェーズから）
  - [タスク名]（[役割名]）理由：[持ち越し理由]

▌アクティブチーム一覧（Claude Code以外の環境用）
| チーム名 | 状態 | 担当ファイル（src/以下） | 現在のタスク |
|---------|------|----------------------|------------|
| [名前]  | 稼働中/待機 | [パス] | [タスク] |

▌BDRインデックス
| ID      | 日付    | タイトル   | タグ    | フェーズ  |
|---------|--------|----------|--------|---------|
| BDR-001 | [日付] | [タイトル] | [タグ]  | Phase N |

PROJECT_STATE.md v1.0（アプリ特化版）— 🔄随時更新層。
セッション開始時に「開発情報」セクションを必ず確認すること。
