# PROJECT_STATE.md — プロジェクト現在地

最終更新：2026-08-25

## フェーズ
Phase 2: 実装完了・外部サービス接続待ち

## 概要
トレカ（BOX/パック単位）の販売相場・買取相場を、Xの複数アカウント投稿（手動投入）から
収集・AI解析・横断比較できるアプリ。要件定義・実装が完了し、動作確認（tsc/lint/guard/build/
dev起動）まで済んだ状態。あとは外部サービス（Neon・Gemini）の接続設定のみ。

## 直近の決定事項
- Xの自動巡回は行わない（有料APIが前提になるため。手動投入方式を採用）
- 認証なし（開発者本人専用）
- スタック：Next.js(App Router) + TypeScript + Tailwind + Neon(Postgres, `@neondatabase/serverless`で直接SQL) + Gemini API(解析)
- 投稿画像はNeonにファイルストレージが無いため、DBにbytea（バイナリ）として直接保存
- データ取得はSWR（useEffect+fetchの直書きは避ける方針。CODE_ANTI_PATTERNS.md AP-A1準拠）

## 実装済み機能
- アカウント管理（複数登録・選択）
- 投稿登録（URL/テキスト/画像の複数投入・一括投入、AI解析、確信度によるレビュー要否判定）
- 商品名の名寄せ（表記ゆれ統合UI）、重複投稿検知（URL一致・内容ハッシュ一致）
- 日別横断参照、アカウント別まとめ（販売⇔買取比較・スプレッド表示）
- 商品別の価格推移グラフ・店舗ランキング
- CSVエクスポート、Xブックマークレット（URL自動取り込み）
- scripts/guard.mjs をこのアプリ向けに書き換え、self-testで実際に検知することを確認済み

## 次のアクション（ユーザー側の作業）
1. Neonプロジェクトを作成し、`db/migrations/0001_init.sql` を実行
2. Gemini APIキーを取得
3. `.env.local` を設定（`.env.local.example` 参照）
4. `npm run dev` で起動し、アカウント登録→投稿登録の一連の流れを実データで確認

## 既知の制約
- 認証なしのため、公開URLへデプロイする場合はホスティング側でアクセス制限が必要
  （guard.mjsのINC-031/INC-035除外コメント参照）
- 商品の名寄せは完全一致（大文字小文字無視）のみ。表記ゆれは手動統合UIで対応
