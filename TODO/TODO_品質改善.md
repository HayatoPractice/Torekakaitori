# TODO_品質改善.md — 作業手順書
作成日：2026-06-21 ｜ 最終更新：2026-06-21

---

## 目的・背景
コードレビューで挙がった9項目の品質・バグ・UX課題に対応する。
調査の結果、大半はコミット 24418bb（前回セッション）で実装済みであり、本セッションでは残課題（#5 画像リサイズ・#7 価格集計のSQL化）と#4の残存箇所を対応した。

---

## 2026-06-21 品質改善対応

### 🔴 優先度：高（バグ・リスク）
- [x] **#1 鑑定詳細ページのServer Component化** `[高]` — 実装済（24418bb）。`diagnoses/[id]/page.tsx`・`history/page.tsx` ともServer Component。
- [x] **#2 uuid パッケージ削除** `[高]` — 実装済。依存・node_modulesに `uuid` なし。全箇所 `crypto.randomUUID()`。
- [x] **#3 estimated_price カラム問題** `[高]` — 問題なし。diagnoses へのINSERTに含まず、`price_log.price` へ記録。スキーマ変更不要。
- [x] **#4 catch (error: any) の統一** `[高]` — APIルートは実装済。残存していた `knowledge/submit/page.tsx` を本セッションで `error instanceof Error` へ統一。

### 🟡 優先度：中（品質向上）
- [x] **#5 画像サイズ制限＋リサイズ** `[中]` — 5MB制限は実装済。本セッションで `src/utils/imageResize.ts` を追加し、長辺1568px・JPEG品質0.85へ圧縮。両アップロード画面に組込。
- [x] **#6 createClient() の毎レンダリング再生成** `[中]` — 実装済。`HistoryList.tsx` は `handleDelete` 内で生成（毎レンダリングではない）。
- [x] **#7 価格集計のJS全件ループ** `[中]` — 本セッションで Postgres関数 `get_price_stats` を追加しRPC化。非プレミアムは集計1回のみ、月別トレンド（全件）はプレミアム時のみ取得。mockClientに `rpc()` 追加。

### 🟢 優先度：低（UX改善）
- [x] **#8 診断履歴の削除** `[低]` — 実装済。`HistoryList.tsx` に削除ボタン・RLS削除動作。
- [ ] **#9 価格データの実売価格蓄積戦略** `[低]` `#戦略` — 未対応。コード課題ではなく、ナレッジ投稿の実売価格をどう促進・蓄積するかの運用戦略。別途検討。

---

## 📅 日次サマリー

### 2026-06-21
- 完了：8件（#1〜#8）
- 残り：1件（#9・戦略課題）
- 特記：`npm run build`（型チェック含む）パス。/api/price-trend は不在、mockClient は現役使用中で削除不可と確認。

---

## 変更履歴
- 2026-06-21：品質改善TODOを作成し、#4/#5/#7 を実装。残りは実装済みとして記録。
