# MINUTES.md — 作業議事録（AI セッション引き継ぎ用）
最終更新：2026-08-01 | セッション #1

---

## ★ TL;DR — 次の AI はここだけ読めば再開できる

| 項目 | 内容 |
|:---|:---|
| このフォルダの役割 | 「アプリ作成原本」= 新規アプリ作成用の共通テンプレート（`scripts/create_new_app.py` の配布元）。**特定アプリの実装コードを置く場所ではない** |
| 現在地 | Claude標準挙動との二重指示排除・VintVerify混入の解消が完了し、テンプレートとして正常化した状態 |
| 前回完了 | ①APP_SHARED_RULES.md §1-6欠番化＋付録A新設＋サブエージェント無断起動禁止の明記、②`/doctor`によるCLAUDE.mdクロスチェック、③誤ってCLAUDE.mdに混入したVintVerify固有内容を全13アプリから復旧、④VintVerifyの実コード（src/・supabase/・package.json等）がこの原本フォルダに直接置かれていた問題を発見し `../古着ブランド・年代鑑定ツール` フォルダ（既存の正規クローン、未コミットの進んだ実装あり）へ統合・原本から完全除去 |
| 次のタスク | 特になし（テンプレートとして正常な状態）。今後もし原本直下に `src/` `package.json` 等アプリ固有ファイルが出現したら、それは異常事態なので調査すること |
| 注意事項 | VintVerifyの開発は今後 `アプリ作成関連/アプリ作成/古着ブランド・年代鑑定ツール` フォルダで行う。そちらのgit remoteは事故防止のため削除済み（push前に専用リポジトリを作成してremote設定すること）。このMINUTES.mdは原本フォルダ自身のルール整備作業専用で、個別アプリの開発ログとは別管理 |

---

## セッション履歴（新しい順）

### SESSION #1 — 2026-08-01

#### タスクログ
- ✅ **Claude標準挙動との二重指示排除** — `APP_SHARED_RULES.md` §1〜6を欠番化（理由表つき）、§10-3「二重指示の禁止」新設、§25-1の世間一般デバッグ手順を削除、付録A「AIツール別自動化差分表」新設。`SERVICE_ORG_CORE.md`にサブエージェント（Explore/Plan/general-purpose）は明示依頼時のみ起動が既定である旨を明記。`debug_check.py`の一般論6項目を削除。
- ✅ **`/doctor`によるクロスチェック** — CLAUDE.mdに冗長だった開発コマンド4行を圧縮。他項目（設定健全性・hooks速度・auto mode・許可ルール）は健全。
- ⚠️ **事故と復旧：CLAUDE.mdへのVintVerify内容混入** — `/doctor`セッションがCLAUDE.mdにVintVerify固有の「コードベース概要」を追加 → post-commitフックで無関係な12アプリに配布される事故が発生。git管理下の8アプリは`git checkout`、git未管理の投資管理アプリは手動コピーで復旧。原本のCLAUDE.mdからも該当セクションを削除。
- ⚠️ **発覚：VintVerifyがこの原本フォルダ内で直接開発されていた** — `package.json`のname が `"vint-verify"`、`src/`・`supabase/`等が原本直下に存在。さらに調査の結果、`../古着ブランド・年代鑑定ツール`フォルダが原本の別クローンとして既に存在し、そちらには2026-06-21に行われた**未コミットのさらに進んだ実装**（商用化設計書・診断ナレッジ機能・パスワードリセット・DBマイグレーション等）があることが判明。
- ✅ **統合・復旧** — `古着ブランド・年代鑑定ツール`側の未コミット変更（26ファイル）をレビューの上コミットして確定。同フォルダのgit remoteは原本と同じ`app-template.git`を指していたため、誤push事故防止のため削除。原本フォルダからVintVerify関連の全ファイル（`src/` `supabase/` `public/` `package.json`等・`MINUTES.md`・`TODO/`のアプリ固有分・`DOCS/REQUIREMENTS_LOG.md`の実データ）を除去し、テンプレートとして初期化し直した。

#### 重要事項
- `#決定` 今後、原本フォルダで特定アプリの実装作業は行わない（テンプレート専用として維持する）。
- `#決定` VintVerifyの開発拠点は `古着ブランド・年代鑑定ツール` フォルダに一本化する。

---

MINUTES.md v1.0 — アプリ作成原本（テンプレート）用
