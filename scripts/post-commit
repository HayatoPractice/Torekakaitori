#!/bin/bash
# post-commit — git commit 完了後に自動実行される Git フック
# インストール方法: bash scripts/install_hooks.sh

REPO_ROOT="$(git rev-parse --show-toplevel)"
SYNC_SCRIPT="$REPO_ROOT/scripts/sync_to_apps.py"

echo "🔄 コミット完了 → 全アプリへ同期します..."
python3 "$SYNC_SCRIPT"
echo "✅ 同期完了"
